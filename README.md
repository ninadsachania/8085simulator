# 8085Simulator

## Required Software

- JDK 8
- Maven

## Build

To build the application:

```shell
mvn clean verify
```

## Run

To run the application, open the JAR file inside the `target/` directory.
